# Copy this into Streamlit Cloud → App settings → Secrets
# (without the .example extension — never commit the real secrets.toml)

TRAVELPAYOUTS_TOKEN = "your_token_here"
GOOGLE_MAPS_API_KEY = "your_key_here"
